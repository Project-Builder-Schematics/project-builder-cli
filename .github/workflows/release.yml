# DEFERRED — see issue #8 for the multi-platform binary release implementation.
# This workflow is intentionally inert: no triggers, no jobs. The file claims
# the canonical path so future work does not bikeshed where the release lives.

name: Release

on:
  workflow_dispatch:

jobs: {}
